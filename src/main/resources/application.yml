spring:
  datasource:
    url: jdbc:mysql://localhost:13306/pilot-temp?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: admin
  #    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

